import vendor/http/http as http
import vendor/bernparsec as bernparsec

http_response = http:get("https://raw.githubusercontent.com/bern-lang/bern/master/tests/import_conflict_alias_ambiguous.brn")
http_body = http_response["body"]
parse_result = bernparsec:parse(bernparsec:program(), http_body)
if (parse_result["ok"]) then
    "Parsed successfully: " + parse_result["value"]
else
    "Parse error: " + parse_result["error"]